# Checkout Course Build Plan

## Scope

Build the intentionally small checkout application defined in `REQUIREMENTS.md`, including its Requirements Update. The update supersedes only the baseline in-memory persistence rule; all other baseline requirements remain active.

## Technology choices

- **Application:** Next.js with the App Router and a `src/` directory.
- **Language:** strict TypeScript.
- **Test runner:** Vitest. It is lightweight, works well with TypeScript, and supports a simple one-shot `test:ci` script without requiring browser automation for the required route tests.
- **Styling:** one global CSS file only; no Tailwind, component library, or CSS-in-JS package.

## Project setup

Create the application with the official Next.js scaffold rather than hand-writing the base project files. From the parent directory, use a command equivalent to:

```sh
npx create-next-app@latest my-app --yes --app --src-dir --typescript --use-npm --no-tailwind
cd my-app
npm run dev
```

The explicit flags retain the requested default Next.js creation workflow while ensuring the scaffold meets the course requirements: App Router, strict TypeScript, a `src/` directory, npm scripts, and no Tailwind. The project root for the baseline application will be `my-app/`; course artifacts should be kept with that project root.

## Files to create

| File | Responsibility |
| --- | --- |
| `package.json` | Generated by `create-next-app`; update it only to add Vitest and the required `test:ci` script. |
| `package-lock.json` | Generated by npm to lock the approved dependency tree for reproducible installs; retain it with the application. |
| `tsconfig.json` | Generated by `create-next-app`; provides strict TypeScript and the `src/` project structure. |
| `next.config.*` | Generated by `create-next-app` when applicable; retain its minimal configuration. |
| `src/app/layout.tsx` | Generated by `create-next-app`; update it to provide the root document layout and import global styles. |
| `src/app/globals.css` | Generated by `create-next-app`; replace scaffold styling with the required system-font styling, one accent colour, max-width layout, consistent spacing, and rounded cards or buttons. |
| `src/app/page.tsx` | Home page with a heading and link to `/checkout`. |
| `src/app/checkout/page.tsx` | Checkout page that constructs and posts a cart to `/api/checkout`; uses the allowed placeholder user ID `guest` if the UI has no user ID field. |
| `src/app/api/products/route.ts` | Handles `GET /api/products` and returns the three hard-coded catalogue products. |
| `src/app/api/checkout/route.ts` | Handles `POST /api/checkout`: parses JSON, validates the cart, calculates server-derived totals, persists a completed order, and returns its ID and total. |
| `src/app/api/orders/route.ts` | Handles `GET /api/orders` and returns the persisted order array. |
| `src/lib/products.ts` | Holds the single in-memory, server-side product catalogue shared by the product and checkout routes. |
| `src/lib/orders.ts` | Uses Node.js file-system APIs to read and append completed orders in `data/orders.json`, while preserving malformed storage content. |
| `src/lib/pricing.ts` | Contains only the approved no-op `calculateDiscount` stub. This initial creation is approved by this plan; future edits require explicit human approval. |
| `data/orders.json` | Holds only course-generated synthetic persisted orders. It is created as an empty JSON array if absent at checkout time. |
| `README.md` | Explains installation, scripts, endpoints, and the request lifecycle. |
| `tests/orders.test.ts` | Covers order persistence, catalogue-priced checkout, invalid input, and safe malformed-storage handling. |
| `vitest.config.*` | Minimal Vitest configuration, if required for the selected TypeScript setup. |
| `.gitignore`, `AGENTS.md`, `CLAUDE.md`, `eslint.config.mjs`, `next-env.d.ts`, `public/*.svg`, `src/app/favicon.ico` | Unmodified scaffold and instruction support files retained from project setup. They provide ignore rules, agent guidance, lint configuration, Next.js type declarations, and default static assets; they add no checkout functionality or external integration. |

## Dependencies and npm scripts

### Dependencies

- Generated by `create-next-app`: `next`, `react`, `react-dom`, `typescript`, `@types/node`, `@types/react`, and `@types/react-dom`.
- Added after scaffolding: `vitest`.

### Required scripts

| Script | Responsibility |
| --- | --- |
| `dev` | Starts the Next.js development server. |
| `build` | Produces a production Next.js build. |
| `test:ci` | Runs Vitest once, without watch mode. |

The completed project must support `npm install && npm run dev`.

## Product catalogue

`GET /api/products` will return exactly this in-memory catalogue:

```json
[
  { "id": "prod-001", "name": "Enamel Mug", "price": 12.5 },
  { "id": "prod-002", "name": "Canvas Tote", "price": 18.0 },
  { "id": "prod-003", "name": "Wool Beanie", "price": 22.75 }
]
```

Prices remain JavaScript floating-point dollars throughout the baseline.

## API implementation and request flow

1. A visitor opens the home page and follows its link to `/checkout`.
2. The checkout page obtains or uses the hard-coded product choices and constructs a request body containing a client-supplied `userId` and cart items.
3. The checkout page sends the body to `POST /api/checkout`.
4. The checkout route parses the body as JSON. Malformed JSON immediately returns HTTP 400 and `{ "error": "<human-readable message>" }`.
5. The route validates that `userId` is a non-empty string and `items` is a non-empty array.
6. For every cart item, the route confirms a known `productId` and a present, positive integer `quantity`. Any invalid cart returns HTTP 400 with the defined error shape.
7. The route resolves every price from `src/lib/products.ts`, never from the request body, then calculates the subtotal as the sum of `unitPrice × quantity`.
8. The route calls `calculateDiscount(subtotal)` from `src/lib/pricing.ts` and calculates `total = subtotal - discount`.
9. The checkout route creates a completed order with a generated ID and ISO-8601 timestamp, then appends it to `data/orders.json` using Node.js file-system APIs. If the file is absent, it first creates an empty JSON array.
10. Because the approved pricing stub returns `0`, the success response is HTTP 200 with `{ "id": string, "total": number }`.
11. `GET /api/orders` reads and returns the current order array. Both order routes use the Node.js runtime. Malformed or non-array order storage returns HTTP 500 with a safe error message without overwriting the file.

## Tests and acceptance checks

- `npm install` completes successfully.
- `npm run dev` starts the Next.js development server.
- `npm run build` completes successfully under strict TypeScript.
- `npm run test:ci` executes the Vitest order-route tests successfully.
- `GET /api/products` responds with HTTP 200 and exactly the specified three products.
- A valid `POST /api/checkout` responds with HTTP 200 and `{ "id": string, "total": number }`, and appends a completed order with the required order/item shape.
- `GET /api/orders` returns HTTP 200 and the current persisted order array.
- Checkout uses catalogue prices only, even if a client sends an extra price field.
- A malformed JSON body returns HTTP 400 with `{ "error": "<human-readable message>" }`.
- Each of the following returns HTTP 400 with the same error shape: missing, empty, or non-string `userId`; missing or empty `items`; an unknown product ID; and zero, negative, non-integer, or missing quantity.
- Malformed or non-array `data/orders.json` returns HTTP 500 with a safe error response and preserves the file contents.
- `src/lib/pricing.ts` exactly contains the approved no-op stub and no other contents.

## Constraints and how they will be satisfied

| Constraint | Plan response |
| --- | --- |
| App Router, strict TypeScript, and `src/` | Use Next.js App Router routes under `src/app` and strict TypeScript configuration. |
| Required endpoints | Create the product read route, the checkout write route, and the updated order read route. |
| Persistence | Store the catalogue in a TypeScript module and completed orders in `data/orders.json` using Node.js file-system APIs; add no database. |
| No authentication | Treat `userId` as a client-supplied non-empty string and add no auth logic. |
| Sensitive pricing module | Create the exact approved stub once; make no later content change without explicit human approval. |
| Floating-point dollars | Use JavaScript numbers and make no rounding, decimal, or minor-unit changes. |
| Minimal styling | Use one global CSS file with a system font, one accent colour, rounded UI, a centered max-width container, and consistent spacing. |
| No extra infrastructure | Add no external services, Docker, database, Tailwind, component library, or CSS-in-JS package. |
| Small scope | Limit application work to the named pages, routes, storage modules, data file, catalogue, pricing stub, README, and order-route tests. |

## Assumptions and risks

- The exact checkout controls are not specified. The UI will stay minimal while ensuring it posts the required request shape.
- The requirements deliberately permit floating-point results; no special monetary rounding will be introduced.
- Validation error messages may vary in wording, provided they are human-readable and use the required HTTP 400 error shape.
- `pricing.ts` is sensitive. Any later request that would alter its exact stub must be paused for explicit human approval before editing.
- JSON-file writes are a single-process training solution; concurrent writes are not guaranteed safe. The README documents this limitation instead of adding a database or external service.
